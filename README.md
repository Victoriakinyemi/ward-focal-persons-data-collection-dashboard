# Ward Focal Persons Data Collection Dashboard
This dashboard tracks data submitted by ward focal persons (WFPs), shows their coverage and flags wards that are below target.

## Files
- `config.json` contains dashboard settings, including target
- `doc/indicator-definitions.md` contains official indicator definitions and target
- `data/sample_coverage.csv` contains realistic synthetic data for testing
